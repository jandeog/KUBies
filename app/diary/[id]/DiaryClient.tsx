'use client';

import * as React from 'react';

export default function DiaryClient({ id }: { id: string }) {
  // προσωρινό stub για να “πρασινίσει” το build
  return <div style={{ padding: 16 }}>Diary #{id}</div>;
}
